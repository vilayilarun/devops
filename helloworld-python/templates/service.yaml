apiVersion: v1
kind: Service
metadata:
  name: {{ include "helloworld-python.fullname" . }}
  labels:
    {{- include "helloworld-python.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helloworld-python.selectorLabels" . | nindent 4 }}
